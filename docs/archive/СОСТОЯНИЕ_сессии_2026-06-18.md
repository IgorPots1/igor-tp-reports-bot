# СОСТОЯНИЕ сессии 2026-06-18 → handoff для новой сессии Claude

Старт новой сессии — с этого файла. Факты из репо (хэши/ветки/строки), не по памяти.
**Ветка `task10d-methodology-bugs`. origin == local, tip `a4f9628`, всё запушено (0/0). Прод/main не трогали.**
Большое предыдущее самари: `PROJECT_SUMMARY_FULL.md` (снято на `70422b2`). Эта сессия — `70422b2 → a4f9628`.

---

## 1. ЧТО ЗАКОММИЧЕНО ЗА СЕССИЮ (11 коммитов, по порядку)

| Хэш | Суть |
|-----|------|
| `6d2a3df` | **Углеводы fast/slow/neutral/not_carb_base** — детерминированный `carb-quality.ts` (`classifyCarbItem`, грамм-гейт `CARB_CONTRIBUTOR_MIN_G=20` → fast→slow→neutral); тег `carb_class` в facts; промпт «класс из факта»; **гард `carb_quality_mismatch` сужен до FAST-only** (на neutral давал 4/9 ложных срезов — рис/паста идиома). |
| `63695cd` | **Две тренировки в день** — суммирование расхода ВСЕХ сессий; общий хелпер `sumDaySessionsExpenditureKcal` (`activity-energy.ts`), review (`methodology.ts` `WorkoutContextByDate.sessions`) + plan (`weekly-plan-formulas.ts`). Одиночные дни байт-в-байт. + диагностика забега Нади (`diagnose-nadya-race.ts`). |
| `c793961` | **Промпт:** пример «крупа или **паста**» (рис убран из примера, не из классификатора); «быстрый углевод НЕ называть „правильным источником“/„углеводной основой“». |
| `ea63ab2` | **Safety-блок → пометки #1** — `buildNutritionSafetyFlags` `blocked=false`, `doNotSendReasons=[]`; убрана override-кнопка/проводка. |
| `090db95` | **Table Tennis / Ping Pong → «настольный теннис»** (в `TRAININGPEAKS_ACTIVITY_RU_MAP` ПЕРЕД `Tennis`); промпт «не сокращать названия продуктов из items_notable». |
| `fb59e3e` | **Юлия Крылова: флаг `exclude_other_activities`** (lose) — зеркало own_regime, фильтр TP-«Other»/«Custom» по сырому `sportOrTypeCode` на одном чокпойнте (`buildNutritionTrainingPeaksWeekContext`); день только-Other → rest. **Миграция `20260618120000`.** |
| `b696d66` | **Safety-блок #2** — `hard_flags=[]` в источнике (сигналы → `softFlags`), т.к. блок реконструировался из hard_flags в 5 местах. |
| `3438a65` | **Имена продуктов** — расклейка кодом (`tidyParsedFoodName`, file-intake: предлоги «сПенне»→«с Пенне», обрезка по границе слова) + **бренд→продукт промптом**; **приветствие без имени** (`resolveGreeting`: ты→«Привет!», вы→«Здравствуйте!»). |
| `a85ebc4` | **Safety-блок #3 (кардинально)** — гейт combine (`isReviewBlockedSafety`/`isPlanBlockedSafety`) игнорирует safety-блок (легаси-статус, `safety.blocked`, `manual_review_required:*`) → **старые строки разблокируются на рендере без перегенерации**. |
| `b76eb90` | **Задача г** — allow-set ориентиров прозы округляет floor/ceil (модель пишет «310» от 315, «~120» от 114–125); фактические макросы строгие. На рендере → лечит сохранённые отчёты. |
| `a4f9628` | **race-day КБЖУ в мини-таблице** — ключ `race` в `FORMULA_BY_DAY_TYPE` (база=hard) + `computeRaceDayTarget` по дистанции из `computeNutritionRaceProtocol` (ПМ+ → loading-high). Было «н/д». |

---

## 2. НА ВЕТКАХ / ЖДЁТ КОММИТА
**Ничего.** Все ветки сессии влиты в task10d (ff) и удалены. Рабочее дерево по трекнутым чистое.
Untracked dev-скрипты (read-only, НЕ коммитить): `diagnose-nutrition-flags.ts`, `СОСТОЯНИЕ_сессии_2026-06-18.md` (этот файл), `SUMMARY_session_2026-06-18.md`, `PROJECT_SUMMARY_FULL.md`, чекпойнты.

---

## 3. ОТКРЫТЫЕ ФИКСЫ С НАЙДЕННЫМ КОРНЕМ (decisive — не передиагностировать)

**A. persist забегов — мост скан↔БД сломан на контракте полей.**
`races.json` (последний 2026-06-18, 15 гонок, вкл. «Ночной Забег» 20.06/10 км) содержит `student_name` + `athlete_id`, **`student_id` НЕТ**. А `persistScannedRaceEvents` ([tools/trainingpeaks-export/scripts/tp-races-requests-once.ts:501-503](tools/trainingpeaks-export/scripts/tp-races-requests-once.ts#L501)) читает `row.student_id` (slug) → `!slug` guard молча `continue` на ВСЕХ строках → таблица `trainingpeaks_race_events` пуста.
**Раннер ЖИВ:** launchagent `com.igor.trainingpeaks-races-loop` (PID 1697), процесс `tp-races-loop --interval-seconds=60` (PID 2518, тикает), 8 джобов `race_scan_events` = completed, 0 в очереди.
**Фикс:** в `persistScannedRaceEvents` резолвить ученика по `athlete_id` → `trainingpeaks_students.trainingpeaks_athlete_id` (или по `student_name`). Тогда автозагрузка заработает на живом раннере без новых компонентов.

**B. Корявые слова — точечный фильтр не масштабируется.**
Расклейка предлогов + промпт-«не сокращать» закрыли часть; CAPS/бренд-словарь сознательно не делали (та же ловушка, что блэклист). Перейти на **подход В: эталоны голоса в `narrative-guardrails.ts` + короткий стоп-лист.**
Накопленный стоп-список: `сухой/суховаты`, `опора`, `зарядный`, `почти двух часов`, `небольшой береговой`, «восстановление поддержать увереннее».

**C. ЗАКРЫТО (фикс готов, ветка `fix-exclude-other-typevalueid` от 978eba6, ждёт «да»/коммита) — Юля exclude_other.**
Корень (НЕ передиагностировать): TP-«Other» приходит как `workoutTypeValueId=100` + `title="Other"` + `sportOrTypeCode=null`. Классификатор мапит по id только 3→run, 9→strength; токены other/custom ищет в коде/снэпшоте, не в title → «Other» = `unknown`. Прежний дискриминатор (`isNutritionExcludedOtherActivity`, context.ts) смотрел только `sportOrTypeCode` (null) + `family==="other"` (unknown) → FALSE → её 2ч-«Other» доживали и по длительности читались как «длительная».
**Фикс:** дискриминатор дополнен (сигналы добавлены, прежние сохранены): (#2) `workoutTypeValueId===100` → excluded; (#3) сырой `title==="Other"` точным сравнением, НО не при реальном id (3/9). До/после на Юле: было 6 сессий + ложная длительная «Other» 1.58ч → стало 4 (только беговые), длительной нет. Чек `check-nutrition-exclude-other-activities` расширен (id=100, бег/силовая с title=Other остаются, «Brotherhood» не матчит). Мария — отдельно (перезальёт правильный PDF, date-mismatch растворится).

**D. Туркина / неполный PDF — РАЗДЕЛИТЬ ДВА СЛОЯ (не путать).**
Корень: неполный PDF (Туркина 4 дня 08-11) → `week_to` разбора = последний день еды (06-11) → план = `review_to+1..+7` = 12-18, сдвинут на 3 дня назад → не дотягивает до текущей недели. Полный PDF (Трофимова 08-14) → план 15-21, корректно. Отличие — только полнота PDF (диагностировано сравнением Туркина vs Трофимова).
- **Слой A (разбор) — СДЕЛАН, ветка `fix-week-calendar-snapping`, ждёт коммита.** Неделя разбора снапается к полной календарной неделе (пн–вс) по большинству распарсенных дней (`snapParsedDatesToCalendarWeek` в `report-date-coverage.ts`; интейк-тайм, не ретроактивно). Туркина 08-11 → 08-14 (при перезаливе); Трофимова 08-14 → байт-в-байт. mismatch-нотис показывает СЫРЫЕ даты PDF (не снапнутые). Данные не выдумываются (разбор по реально распарсенным дням). Чек `check-nutrition-report-date-source` обновлён.
- **Слой B (план-окно) — ОТЛОЖЕН до формы обратной связи (правило не устоялось).** Идея: план = `upload_date+1 .. upload_date+7` (от дня сдачи отчёта, переменная длина/скользящее окно), TP-контекст следующей недели — на те же даты; брать сохранённый `created_at`, не `now()`. Текущее поведение (`review_to+1..+7`, `tpNextWeek`/`planWeekFrom`) НЕ тронуто. Standalone-генератор уже умеет календарную неделю от сегодня (`resolveNutritionPlanTargetWeek`), комбинированный поток берёт `review_to+1` (`draft-generator.ts:1556` ← `context.tpNextWeek.periodFrom`, `context.ts:1003-1007`).

---

## 4. РУЧНЫЕ ДЕЙСТВИЯ ИГОРЯ (не код)
1. **Применить миграцию** `20260618120000_add_nutrition_exclude_other_activities.sql` на превью.
2. **Юлия** — в карточке: `goal_type=lose` + галочка «Игнорировать активности типа Other».
3. **Убедиться, что превью пересобрано на `a4f9628`** — фиксы блока/прозы/имён работают НА РЕНДЕРЕ (лечат сохранённые отчёты без перегенерации) только на свежем коде.
4. **Старты** — пока persist не починен: ручная отметка в карточке «Старты» (source=manual) → перегенерация. После фикса persist — перепрогон скана наполнит таблицу автоматически.
5. Проверить на превью: Утенкова/Чернышева (блок снят), Maria (проза 4/4), Yulia (5 дней), race-day мини-таблица (не «н/д»).

---

## 5. ДИАГНОСТИРОВАНО, КОДА НЕ ТРЕБУЕТ
- **Трофимова** — день отдыха «ОК» при высокой ккал: goal=maintain, перебор дня отдыха не штрафуется (by design); maintain-якорь `35×вес` задирает лёгких → carry-forward «пересчёт maintenance-базы для maintain по всем».
- **Семешина** — целевая по НОРМЕ (`goal_day_target` = RMR+расход−дефицit, не подтянута к факту), факт низкий = энергодоступность, не баг. Проверить: поднимался ли hard-флаг при ≥2 днях <1300 (но hard-блок мы и так сняли — теперь это пометка). Скрипт `diagnose-nutrition-flags` теперь печатает факт vs ориентир-цель vs EA-пол.
- **Полякова** — ученица закрыта (мед.причина, решено Игорем). own_regime работает.

---

## 6. ПАМЯТЬ (S3 — by design, НЕ баг)
Генерация читает как историю ТОЛЬКО `approved_patterns` ([draft-generator.ts:1223,1263](src/features/nutrition/draft-generator.ts#L1223)); прошлый ТЕКСТ (ни авто, ни финал) не читает. `previous_weeks_context` строится только из approved_patterns или `null` ([:1850](src/features/nutrition/draft-generator.ts#L1850)) — фактически мёртвый код для сравнения недель. **Финал Игоря нигде не сохраняется** (`coach_edits` всегда null, экшена сохранения нет; черновик в UI readonly — копипаст).
**Хочется (MVP):** сравнение неделя-к-неделе — код считает дельты из `nutrition_weekly_analyses`, модель проговаривает. **НЕ удалять `nutrition_weekly_analyses`** — на нём строится эта память.

---

## 7. ОЧЕРЕДЬ ПРИОРИТЕТОВ
1. **Старты:** race-day уже закоммичен (`a4f9628`); осталось — **фикс persist по `athlete_id`** (раздел 3A).
2. **Диагностика 1–2 учеников** — Игорь даст имена (`diagnose-nutrition-flags.ts` / `diagnose-nutrition-generation-mode.ts`).
3. **Telegram mini app Слой 1** — каркас РЕАЛИЗОВАН (некоммичен, ждёт «да»). Слои 2–3 далее.
4. **Память MVP** — сравнение неделя-к-неделе (раздел 6).

### Mini app Слой 1 — что сделано (ветка task10d, некоммичено)
- `supabase/migrations/20260619120000_add_trainingpeaks_students_telegram_user_id.sql` — колонка `telegram_user_id bigint`
- `src/features/telegram/validate-init-data.ts` — HMAC initData по TELEGRAM_BOT_TOKEN
- `src/features/telegram/types.ts` — `TelegramWebAppInfo`, `TelegramWebAppInlineKeyboardButton`
- `src/features/telegram/telegram-client.ts` — `sendTelegramWebAppButton`
- `src/features/trainingpeaks/repository.ts` — `telegramUserId` в типе + `getTrainingPeaksStudentByTelegramUserId`
- `src/app/m/layout.tsx` — Telegram SDK (`strategy="beforeInteractive"`)
- `src/app/m/n/page.tsx` — 2-step клиентский компонент (выбор PDF → preview → confirm → close)
- `src/app/api/m/n/upload/route.ts` — парс PDF, snapping, preview без сохранения
- `src/app/api/m/n/confirm/route.ts` — `saveNutritionFileReport` + нотификация Игорю
- `src/app/api/admin/send-nutrition-button/route.ts` — GET?studentId=X, Bearer auth
- `scripts/check-miniapp-init-data.ts` — unit tests HMAC + snapping (PASS)
- Гейт: `MINIAPP_ENABLED=true` (по умолчанию false — ученицы не получают кнопку)

**Что Игорю сделать вручную для запуска:**
1. Применить миграцию `20260619120000_...`
2. Заполнить `telegram_user_id` в карточке каждой ученицы (Telegram → личный user_id ≠ business chat_id)
3. BotFather: задать Web App URL = `https://<domain>/m/n` и menu button
4. Поставить `MINIAPP_ENABLED=true` в Vercel env (preview → проверить → prod)
5. Поставить `NEXT_PUBLIC_BASE_URL=https://<domain>` (или `https://preview.vercel.app`)
6. Отправить кнопку одной ученице: `GET /api/admin/send-nutrition-button?studentId=X` (Bearer ADMIN_ACCESS_TOKEN)

---

## 8. ДИСЦИПЛИНА (соблюдать)
- «Код считает — модель пишет». Наряды средние + чекпойнт на каждый.
- **Не коммитить/пушить/деплоить без явного «да» Игоря.** Поток: ветка от task10d → правки + чеки → отчёт → «да» → коммит → ff task10d (ff-only) → push (fast-forward, без force).
- AI-прогон ≤1 на чекпойнт; структуру проверять детерминированно.
- maintain байт-в-байт; safety (EA-пол, методика lose) не обходить — но **hard-БЛОК снят ОСОЗНАННО по решению Игоря, обсуждённому с ученицами** (сигналы = пометки, не стена; чаще ловил НЕПОЛНОЕ логирование, не голодание; стена мешала дать обратную связь — основную ценность). **Это НЕ баг — НЕ возвращать блок.** Снят только БЛОК; сигналы/пометки и **EA-пол ОСТАЮТСЯ** (EA-пол и методика lose НЕ тронуты). Human-in-the-loop: критичное Игорь видит в пометках и направляет к врачу. Семешина (5 дней <1300, EA красная) → разбор с пометкой «критически мало, повторять нельзя», а не блок = by design. Миграции применяет Игорь.

---

*Собрано 2026-06-18 из репо. tip `a4f9628`, origin == local. Этот файл — untracked, не коммитить без «да».*
