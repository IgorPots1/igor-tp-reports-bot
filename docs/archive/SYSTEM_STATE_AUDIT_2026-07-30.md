# Карта состояния системы — read-only аудит 2026-07-30

6 параллельных аудиторов прошли по всем доменам (не только фидбек). Линза: «тихо врёт / тихо теряет / тихо стоит». Всё read-only — **ничего не менял**. Приоритеты: **P0** опасно сейчас · **P1** реальная дыра · **P2** мелочь/латентно.

## 🔴 ГЛАВНОЕ — требует твоего действия (P0 / P1)

**P0 — два прод-крона Vercel молча умерли в один день (2026-07-28) и никто не оповещён:**
- `billing-email-import` (реальные платежи, ежедневно 08:30 UTC) — последний успех **28.07 08:45**, дальше пусто. Импорт платежей стоит.
- `attention-digest` (утренняя сводка тренеру, 09:00 UTC) — последний **28.07 09:43**, пропущены 29 и 30.
- Оба умерли в один день → почти наверняка проблема **на стороне Vercel** (план/инвокация крона), из репозитория не проверяется — **глянь Vercel dashboard → Cron tab**. `vercel.json` цел.
- Смягчение по биллингу: импорт самозалечивается (lookback 7 дней + дедуп по хэшу) — когда крон оживёт, пропущенные дни доберутся. Но пока крон мёртв — платежи не импортируются, и экспайр сигналов/памяти висит (он прицеплен к digest-крону: **20 operational-сигналов просрочены, но активны**).
- GitHub Actions бэкап дайджеста (должен подстраховывать) за 30 дней доставил **0 раз** — не работает (секреты/воркфлоу, из репо не видно).

**P1 — сравнение: гонки уходят в базу по ТОЧНОЙ дате (off-by-one).** 13 из 254 гонок (5%) реально пробежаны на `event_date ± 1 день` (ночные старты/TZ) → максимальное усилие остаётся в базе сравнения/личном коридоре → норма завышается → потом читается как «регресс». Тот же промах метит рекорд гонки как `training_split`. Фикс: ±1 день в `fetchRaceKeys` и `classifyRecordType`. (Пересекается с недавней race-provenance работой — один узел, одна сессия.)

**P1 — переносы: одобренные ходы молча замерзают.** 3 approved-хода застряли в `dry_run_running` с 30.06/02.07/08.07 — нет reclaim для этого статуса (есть только для `running_local`) → ход никогда не исполнится и никогда не упадёт. Плюс stuck-detection в дайджесте **отключён** (комментарий в коде), failed-ходы старше 48ч выпадают из внимания. Реального «увёл не туда» НЕ найдено — групповая защита fail-closed, подтверждение тренера обязательно.

**P1 — память тренера: устаревшее лечится как свежее.** 409 memory-items с `valid_until=null` не экспайрятся никогда и вливаются в промпт reply-draft **без метки возраста** (пример: «Данил сообщает о болезни», last seen 31.05, ~60д — модель видит как актуальное). Фидбек-путь это уже решил (окно 1 день), reply-draft — нет. Плюс 190 дублирующихся кластеров (дедуп покрывает 3 типа из 11, модуль слияния существует но не подключён).

**P1 — health-метрики: профили стоят, скан застрял.** `next_full_check_at` пишется, но никто не действует по нему (6 профилей просрочены; Дарья 66д, а `status=ready`). У 3 учеников последний health-скан = 403 (кэш заморожен), ничто не алертит.

**P1 — recovery-alert посчитан, но не показан.** `snapshot.observe` считается ежедневно (56 учеников), но **его никто не читает** — ни дайджест, ни desk, ни admin. Проактивно мёртв (виден только реактивно при открытии reply-draft). Пороги ещё и абсолютные (6.5ч/35), не персональные.

**P1 — launchd (Mac-кроны) битые:**
- `race-scan-enqueue` (еженедельно) — падает КАЖДЫЙ раз: `Missing script: enqueue-race-scan-weekly` (скрипт переименован); heartbeat `race_scan` не писался НИ РАЗУ (маскируется 9-дневным race_events fallback).
- `club-materialize-full` (ежедневно 04:20) — `Permission denied`: у скрипта слетел +x → exit 126, не мониторится.
- `stale-job-sweeper` (сеть восстановления зависших джобов) — **не запланирован нигде** (1 запуск, 66 дней назад). Латентно: зависший weekly/race-джоб не восстановится.

## Полная карта по доменам

### 1. Переносы тренировок
| Модуль | Проверил | Итог | Prio |
|---|---|---|---|
| dry_run_running reclaim | застревание хода в дедран | ДЫРА (3 зависших с июня/июля) | P1 |
| digest move_failed_action | всплывают ли зависшие/failed | ДЫРА (stuck-detect выключен, 48ч отсечка) | P1 |
| live_primary PUT vs DOM id | сверка id перед мутацией | ДЫРА (shadow-таблицу никто не читает; 0/117 mismatch эмпирически) | P1 |
| group-move-safety | ход из группы без пруфа кто/чей | РАБОТАЕТ (fail-closed, 3 гейта) | — |
| confidence/source-date гейты | обходимы ли | РАБОТАЕТ (0.8/0.7, inferred блокируется) | — |
| подтверждение тренера | авто-исполнение без approve | РАБОТАЕТ (auto-queue выключен) | — |
| runner liveness | жив ли loop | РАБОТАЕТ (pid жив, ходы completed сегодня) | — |
| fingerprint_exact | достигается ли сильнейший матч | ДЫРА (dead no-op, всё через fields_unique) | P2 |
| execute_pending сироты, логи 53МБ, 33 needs_review | — | мелочь/латентно | P2 |

### 2. Память учеников
| Модуль | Проверил | Итог | Prio |
|---|---|---|---|
| reply-draft инъекция памяти | устаревшее как текущее | ДЫРА (409 null-expiry, без метки возраста) | P1 |
| дедуп/слияние | дубли/противоречия на ученика | ДЫРА (190 кластеров, 3/11 типов, merge не подключён) | P1 |
| group-topic атрибуция | чужой ученик | ДЫРА латентная (0 живых) | P2 |
| expire cron | экспайр по расписанию | ДЫРА (null-expiry не чистится; 7 просроченных активны) | P2 |
| фидбек-путь (fatigue-cause) | стейл-память в фидбеке | РАБОТАЕТ (окно 1 день) | — |
| group-bound containment | health/emotional в групповой | РАБОТАЕТ (health_status дропается) | — |
| экстракция liveness | пишется/читается | РАБОТАЕТ (331 строк/7д) | — |

### 3. Сигналы / алерты / напоминания
| Модуль | Проверил | Итог | Prio |
|---|---|---|---|
| attention_digest каденс | шлётся ли ежедневно | ДЫРА (пропущено 3 из 4 дней, молча) | P1 |
| GH Actions бэкап дайджеста | доставляет ли | ДЫРА (0 доставок/30д) | P1 |
| expire piggyback на digest | единая точка отказа | ДЫРА (20 сигналов просрочены-активны) | P1 |
| recovery-alert рендер | доходит ли до тренера | ДЫРА (snapshot.observe никто не читает) | P1/P2 |
| stale-job-sweeper | вызывается ли | ДЫРА (сирота, 66д) | P2 |
| club payment reminders | подключены ли | ДЫРА (built, never wired) | P2 |
| signal zombie (null expiry) | чистятся ли | ДЫРА (18 активных, 2 по 58д; флаг OFF) | P2 |
| contact/silence tracking | свежесть, покрытие | РАБОТАЕТ (113 строк, свежо, 16 idle≥3д корректно) | — |

### 4. Кроны / службы / heartbeat
| Модуль | Проверил | Итог | Prio |
|---|---|---|---|
| billing-email-import (Vercel) | свежесть, алерт | ДЫРА (стоп 28.07, реальные платежи) | **P0** |
| attention-digest (Vercel) | свежесть | ДЫРА (стоп 28.07) | P1 |
| race-scan-enqueue (launchd) | exit code | ДЫРА (Missing script каждый раз) | P1 |
| club-materialize-full (launchd) | perms | ДЫРА (Permission denied, нет +x) | P1 |
| stale_job_sweeper | расписание | ДЫРА (нигде не запланирован) | P1 |
| heartbeat-monitor покрытие | что мониторится | ДЫРА (не следит за billing/digest/club/etc) | P2 |
| feedback-queue | застрявшие generating | РАБОТАЕТ (0 зависших) | — |
| Mac scan flows (cache/fit/health/enqueue) | heartbeats | РАБОТАЕТ (все свежие) | — |
| watchdog + loop workers | живы ли PID | РАБОТАЕТ | — |
| cron-route auth | гейты | РАБОТАЕТ (Bearer CRON_SECRET, 401) | — |

### 5. Health-метрики + гонки/календарь
| Модуль | Проверил | Итог | Prio |
|---|---|---|---|
| race exclusion (точная дата) | ±1 день гонки в базе | ДЫРА (13 гонок инфлейтят норму + мислейбл рекорда) | P1 |
| health profiler standstill | next_full_check_at энфорсится | ДЫРА (6 просрочены, Дарья 66д ready) | P1 |
| health-scan застрял (403) | последний скан = fail | ДЫРА (3 ученика, кэш заморожен, не алертит) | P1 |
| availability-флаги стейл | флаг vs кэш | ДЫРА (30/86 pulse стейл; смягчено окном чтения) | P2 |
| race_events дубли | manual+scan | ДЫРА (2 пары, может дублить в списке) | P2 |
| distance_km null | гейт сегмент-vs-старт | ДЫРА деградированная (17% null) | P2 |
| health cache filling | наполняется ли | РАБОТАЕТ (свежо сегодня) | — |
| baselines n≥3 | не фейрит на n=1 | РАБОТАЕТ | — |
| race scanner / calendar | события/дропы | РАБОТАЕТ (332 события, календарь чист) | — |

### 6. Мини-апп (Сегодня/Старты) + биллинг
| Модуль | Проверил | Итог | Prio |
|---|---|---|---|
| billing reconciliation | matched↔monthly | ДЫРА изолированная (1 платёж matched, но июньский счёт pending → может пере-напомнить как неоплату) | P2 |
| «Сегодня» overflow >20 | показывает ли «+N ещё» | ДЫРА латентная (бот показывает, мини-апп теряет молча; сейчас все <20) | P2 |
| «Старты» name join | фильтр активных | ДЫРА латентная (нет is_active; 0 живых призраков) | P2 |
| operational-signals cap 200 | дроп старых активных | НЕ-ПРОВЕРЯЕМО латентно (сейчас 79<200) | P2 |
| billing import | жив ли | РАБОТАЕТ (самозалечивается, но крон стоит — см. P0) | — |
| double-alloc/дубли/негативы | инварианты | РАБОТАЕТ (0 нарушений) | — |
| Today tz/boundary + ordering | off-by-one, вразнобой | РАБОТАЕТ (Europe/Belgrade, детерминир.) | — |

## Сводка
- **P0: 1** (Vercel-кроны мертвы — биллинг + дайджест; действие на стороне Vercel).
- **P1: ~9** (сравнение off-by-one, зависшие ходы + невидимость, стейл-память, health standstill+403, recovery-alert мёртв, 3 битых launchd, дайджест-каденс).
- **P2: ~15** (латентные/мелкие).
- **Здоровые ядра:** групповая безопасность переносов, confidence-гейты, подтверждение тренера, фидбек-очередь, Mac scan-flows, watchdog, cron-auth, биллинг-инварианты, мини-апп tz/ordering.

Ни одного «тихо увёл не туда / тихо списал дважды» в исполнении не найдено — дыры в основном класса **невидимость** (посчитали, но не показали / встало, но не алертит). Это и опасно: система не врёт в моменте, но копит слепые зоны.

Скажи, что берём в фиксы и в каком порядке — я делал только карту, код не трогал.
