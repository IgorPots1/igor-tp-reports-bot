/**
 * ТЕКСТ СТРАНИЦЫ /connect. ЭТОТ ФАЙЛ — ДЛЯ ИГОРЯ.
 *
 * Всё, что человек читает на странице, лежит здесь, и только здесь. Правится
 * без единой строчки разметки: меняете текст между кавычками, и всё.
 *
 * Чего здесь НЕТ и быть не должно: списка галочек по устройствам. Он живёт в
 * features/intervals/device-guide.ts, потому что тот же список показывается в
 * мини-приложении, и две копии разъехались бы молча.
 *
 * ── КУДА ПИСАТЬ ─────────────────────────────────────────────────────────────
 *   introRu       «Что входит», «Правила, без которых не работает», «Шаг 1»
 *                 вместе с подразделом про максимальный пульс
 *   afterStepsRu  шаги 3, 4, 5
 *   troubleRu     «Что делать, если»
 *   closingRu     короткая подпись
 *
 * МЕЖДУ introRu И afterStepsRu СТРАНИЦА САМА СТАВИТ ШАГ 2. Он приходит целиком
 * из device-guide.ts: заголовок, подводка, галочки по моделям часов и блок про
 * отсутствие часов. Поэтому нумерация шагов в тексте сходится: после шага 1
 * идёт шаг 2, которого в этом файле нет, а дальше шаг 3 из afterStepsRu.
 *
 * ── РАЗМЕТКА ────────────────────────────────────────────────────────────────
 * Текст здесь в markdown, и он выживает при выводе:
 *   ## заголовок   ### подзаголовок   - список   1. нумерованный
 *   **жирный** внутри абзаца   [ссылка](адрес)
 * Пустая строка разделяет абзацы.
 *
 * Чего разметчик не умеет (таблицы, цитаты, картинки, блоки кода), он НЕ
 * выбрасывает молча: проверка check:connect-page падает и называет строку.
 *
 * Пустая строка в слоте означает «блок не показывать»: так можно выкладывать
 * текст по частям.
 */

export const CONNECT_PAGE = {
  /** Заголовок страницы и вкладки браузера. */
  titleRu: "Как подключить часы",

  /** Строчка над заголовком. */
  eyebrowRu: "подключение · intervals.icu",

  /**
   * ЗАЧЕМ ПОДКЛЮЧАТЬ. Два-три предложения человеческим языком.
   * ТЕКСТ ИГОРЯ — замените этот абзац своим.
   */
  introRu:
    "Тренер строит план по вашим тренировкам, поэтому первым делом нужно, чтобы они к нему " +
    "приходили. Это делается один раз и занимает пару минут: вы разрешаете Intervals.icu " +
    "отдавать данные с часов, и дальше всё происходит само.",

  /**
   * ЧТО ДАЛЬШЕ. Показывается под списком устройств.
   * ТЕКСТ ИГОРЯ.
   */
  afterStepsRu:
    "После этого вернитесь в приложение и нажмите «Я подключил, проверить». Если тренировки " +
    "за последние две недели нашлись, вы увидите их количество сразу.",

  /**
   * ЕСЛИ НЕ ПОЛУЧИЛОСЬ.
   * ТЕКСТ ИГОРЯ.
   */
  troubleTitleRu: "Если что-то пошло не так",
  troubleRu:
    "Самая частая причина — не отмечена галочка про скачивание тренировок. Проверьте её " +
    "первой: без неё подключение выглядит успешным, а данные не идут. Если всё отмечено, а " +
    "тренировок нет через сутки, напишите тренеру.",

  /**
   * ПОДПИСЬ ВНИЗУ.
   * ТЕКСТ ИГОРЯ. Пустая строка — блок не показывается.
   */
  closingRu: "",
} as const;
