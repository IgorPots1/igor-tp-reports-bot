import { redirect } from "next/navigation";

// Корень ведёт на ПУБЛИЧНУЮ страницу, а не в админку. Голый igorp.run — это то,
// что вставляют в шапку профиля и кидают в мессенджер: там должен открываться
// клуб, а не форма входа (её же превью иначе и собиралось бы при шеринге).
//
// Редирект временный (307), а не постоянный: 308 браузеры кэшируют навсегда, и
// вернуть корень в админку потом было бы больно. Админка — по /admin.
export default function HomePage() {
  redirect("/landing");
}
